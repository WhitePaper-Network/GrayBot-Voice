const utils = require("./utils.js");

module.exports = {
	async changeSettings(interaction, readData) {
		let embed = new EmbedBuilder()
			.setTitle("設定画面")
			.setDescription("変更する設定を選択してください")
			
		
	}
}